<div>
  <h2>本当に退会しますか？</h2>
  <div>
    <p>退会する場合は、『退会する』をクリックしてください。</p>
  </div>
  <div>
    <%= link_to '退会しない', customer_path(current_customer.id) %>
    <%= link_to "退会する", withdraw_customer_path(current_customer.id), method: :patch %>
  </div>
</div>