<div class="container-fluid">
  <div class="row">
    <div class="col"></div>
    <table class="col mt-5">
      <tr>
        <td><%= attachment_image_tag @cuisine, :cuisine_image, :fill, 100, 100, format: 'jpeg' %></td>
        <td></td>
      </tr>
      <tr>
        <td>料理名</td>
        <td><%= @cuisine.cuisine_name %></td>
      </tr>
      <tr>
        <td><p>調理時間</p></td>
        <td><%= @cuisine.time %></td>
      </tr>
      <tr>
        <td><p>説明</p></td>
        <td class="overflow-wrap"><%= @cuisine.introduction %></td>
      </tr>
       <tr>
         <th><%= link_to "投稿削除", admins_cuisine_path(@cuisine), method: :delete, "date-confirm" => "本当に削除しますか？" %></th>
       </tr>
    </table>
    <div class="col-1"></div>
    <table class="col mt-5">
      <tr>
        <td><h3>ジャンル一覧</h3></td>
      </tr>
      <% @genres.each do |genre| %>
      <tr>
        <td>
          <%= link_to edit_admins_genre_path(genre) do %>
          <%= genre.genre_name %>
          <% end %>
        </td>
      </tr>
       <% end %>
    </table>
    <div class="col"></div>